# Implement the approved plan

Read the approved spec at {{artifact:spec.md}} and its strict manifest at
{{artifact:manifest.json}}. Implement the task in the current worktree, following the
repository's conventions and keeping the manifest's mechanical boundaries visible.

Run the manifest's verification commands. Write a concise implementation handoff and
a non-empty record of intentional deviations, blockers, and implementation judgments
to: {{produces}}. If there were no deviations, state that explicitly.

When every declared output is written, end your final message with exactly:
"This hop is done — exit this session (/exit or Ctrl-D) to continue the relay."
